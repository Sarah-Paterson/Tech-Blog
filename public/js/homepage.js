// const postCommentBtn = document.querySelector('.post-comment-btn');
const addCommentContainer = document.querySelector('.add-comment');

if (loggedIn) {
    addCommentContainer.classList.remove('is-hidden')
}

// function createComment() {

// }

// postCommentBtn.addEventListener('click', createComment);